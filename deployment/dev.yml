version: "3"
services:
  web:
    container_name: cts
    build:
      context: /home/ubuntu/Codes/scibiz/cashtokens-studio
      dockerfile: ./deployment/dev.Dockerfile
    env_file:
      - .env.dev
    ports:
      - "3001:3001"
